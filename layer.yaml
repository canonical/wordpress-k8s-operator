includes:
  - 'layer:caas-base'
  - 'layer:status'
    
  # Don't need this, as we are not using resources.
  # - 'layer:docker-resource'

  - 'interface:pgsql'
repo: git+ssh://git.launchpad.net/generik8s-charm
